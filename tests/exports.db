========= add.db =======

const const a = 10!
export a to "main.db"!

fn add(a, b) => {
    return a + b!
}

export add to "main.db"!

class Adder {
    function add(a, b) => {
        return a + b!
    }
}

export Adder to "main.db"!

====== main.db =======

import a!
print(a)!

import add!
print(add(1, 2))!

import Adder!
const const adder = new Adder()!
print(adder.add(33, 36))!